[package]
name = "domain"
description = "Tipos puros do domínio: ids, ponto fixo, calendário, RNG determinístico, atributos. Sem I/O, sem tempo de sistema, sem ponto flutuante em caminho de estado (ADR 0002)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
rand_core = { workspace = true }
rand_pcg = { workspace = true }

[lints.clippy]
float_arithmetic = "deny"
