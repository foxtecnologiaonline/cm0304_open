name: managerfc
description: >
  Interface do ManagerFC (Windows, Android, iOS) — cliente Flutter do núcleo
  headless em Rust. Ver docs/02-arquitetura.md e app/README.md.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
